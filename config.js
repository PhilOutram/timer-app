const CONFIG = {
  version: "1.2.0",
  defaultTime: 60, // seconds — standard casual Rummikub turn time
  timeStep: 5,     // seconds per increment/decrement
  minTime: 10,     // minimum allowed turn time
  maxTime: 300,    // maximum allowed turn time (5 minutes)
};
